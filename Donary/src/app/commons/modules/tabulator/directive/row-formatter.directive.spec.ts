import { RowFormatterDirective } from './row-formatter.directive';

describe('RowFormatterDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new RowFormatterDirective();
  //   expect(directive).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
