import { ComponentFixture, TestBed } from '@angular/core/testing';

import { EditSkeletonLoaderComponent } from './edit-skeleton-loader.component';

describe('EditSkeletonLoaderComponent', () => {
  // let component: EditSkeletonLoaderComponent;
  // let fixture: ComponentFixture<EditSkeletonLoaderComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [EditSkeletonLoaderComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(EditSkeletonLoaderComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
