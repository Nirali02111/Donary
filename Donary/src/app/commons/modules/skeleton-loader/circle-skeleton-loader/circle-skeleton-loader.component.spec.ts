import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CircleSkeletonLoaderComponent } from './circle-skeleton-loader.component';

describe('CircleSkeletonLoaderComponent', () => {
  // let component: CircleSkeletonLoaderComponent;
  // let fixture: ComponentFixture<CircleSkeletonLoaderComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [CircleSkeletonLoaderComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(CircleSkeletonLoaderComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
