import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SkeletonInfoLoaderComponent } from './skeleton-info-loader.component';

describe('SkeletonInfoLoaderComponent', () => {
  // let component: SkeletonInfoLoaderComponent;
  // let fixture: ComponentFixture<SkeletonInfoLoaderComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [SkeletonInfoLoaderComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(SkeletonInfoLoaderComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
