import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ScheduleOfTransactionComponent } from './schedule-of-transaction.component';

describe('ScheduleOfTransactionComponent', () => {
  // let component: ScheduleOfTransactionComponent;
  // let fixture: ComponentFixture<ScheduleOfTransactionComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [ScheduleOfTransactionComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(ScheduleOfTransactionComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
