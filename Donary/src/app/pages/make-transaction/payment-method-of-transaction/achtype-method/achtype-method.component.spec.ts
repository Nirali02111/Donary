import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ACHTypeMethodComponent } from './achtype-method.component';

describe('ACHTypeMethodComponent', () => {
  // let component: ACHTypeMethodComponent;
  // let fixture: ComponentFixture<ACHTypeMethodComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [ACHTypeMethodComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(ACHTypeMethodComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
