import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WalletTypeMethodComponent } from './wallet-type-method.component';

describe('WalletTypeMethodComponent', () => {
  // let component: WalletTypeMethodComponent;
  // let fixture: ComponentFixture<WalletTypeMethodComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [WalletTypeMethodComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(WalletTypeMethodComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
