import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreditCardTypeMethodComponent } from './credit-card-type-method.component';

describe('CreditCardTypeMethodComponent', () => {
  // let component: CreditCardTypeMethodComponent;
  // let fixture: ComponentFixture<CreditCardTypeMethodComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [CreditCardTypeMethodComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(CreditCardTypeMethodComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
