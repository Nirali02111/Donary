import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AdvancedFieldsComponent } from './advanced-fields.component';

describe('AdvancedFieldsComponent', () => {
  // let component: AdvancedFieldsComponent;
  // let fixture: ComponentFixture<AdvancedFieldsComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [AdvancedFieldsComponent]
  //   })
  //   .compileComponents();
    
  //   fixture = TestBed.createComponent(AdvancedFieldsComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  // it('should create', () => {
  //   expect(component).toBeTruthy();
  // });

  it('should create the app', () => {
    expect(true).toBeTruthy();
  });
});
